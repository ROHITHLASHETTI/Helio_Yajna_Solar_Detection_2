{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm --prefix helio-yajna-frontend install && npm --prefix helio-yajna-frontend run build",
  "outputDirectory": "helio-yajna-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
